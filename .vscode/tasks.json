{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Build Jukebox Receiver (Debug)",
        "type": "shell",
        "command": "bazel build //main:jukebox_receiver -c dbg",
        "windows": {
          "command": "bazel build //main:jukebox_receiver --experimental_enable_runfiles -c dbg"
        },
        "osx": {
          "command": "bazel build //main:jukebox_receiver -c dbg --spawn_strategy=standalone"
        },
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "problemMatcher": []
      }
    ]
  }